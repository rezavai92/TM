import { AcceptFormatPipe } from './accept-format.pipe';

describe('AcceptFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AcceptFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
