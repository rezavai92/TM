import { AcceptFileFormatPipe } from './accept-file-format.pipe';

describe('AcceptFileFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AcceptFileFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
